package pe.edu.vallegrande.agedetector.model.entity;

public enum MessageType {
    TEXT, // Para compatibilidad con el código existente
    CHATGPT_REQUEST,
    CHATGPT_RESPONSE,
    IMAGE_REQUEST,
    IMAGE_RESPONSE
}